[pytest]
pythonpath = .
addopts = -m "not integration"
markers =
    integration: live tests that call OpenRouteService (require ORS_API_KEY in .env for CLI/core tests; HTTP tests pass apiKey in payload)
